import { ReactNode } from 'react';
import { ElementProps, ImageProps } from '@mantine/core';

export interface ImagePropsType extends ImageProps, ElementProps<'img', keyof ImageProps> {
    children?: ReactNode;
}
export declare function Image(props: ImagePropsType): import("react/jsx-runtime").JSX.Element;
export default Image;
