import { ButtonProps, ElementProps } from '@mantine/core';
import { ReactNode } from 'react';

export interface ButtonPropsType extends ButtonProps, ElementProps<'button', keyof ButtonProps> {
    children?: ReactNode;
}
export declare function Button(props: ButtonPropsType): import("react/jsx-runtime").JSX.Element;
export default Button;
