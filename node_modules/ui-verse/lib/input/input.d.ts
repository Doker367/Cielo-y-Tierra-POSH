import { ElementProps, InputProps } from '@mantine/core';
import { default as React, ReactNode } from 'react';

export interface InputPropsType extends InputProps, ElementProps<'input', keyof InputProps> {
    children?: ReactNode;
}
export declare const Input: React.FC<InputPropsType>;
export default Input;
